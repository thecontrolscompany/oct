# Backend URL for production deployment.
# Leave empty (or omit this file) for local development — Vite proxies /api and /ws to localhost:3001.
# Set to your Cloudflare Tunnel URL when deploying the UI to Vercel.
# VITE_API_URL=https://your-tunnel.your-domain.com
